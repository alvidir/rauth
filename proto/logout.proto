syntax = "proto3";
option go_package = "github.com/alvidir/mastermind/proto/session";

package session;

// LogoutRequest description
message LogoutRequest {
  string key = 1; // required: identifies the user's session
}