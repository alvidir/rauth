syntax = "proto3";
option go_package = "github.com/alvidir/tp-auth/proto/session";

package client;

// LoginRequest description
message LoginRequest {
  string ident = 1;    // the username/appname or email/address
  string pwd = 2;     // the password or the signed public-key
}