syntax = "proto3";

package session;

message LoginRequest {
  string identifier = 1;
  string password = 2;
  string otp = 3;
}

message Empty {}

service Session {
  rpc Login(LoginRequest) returns (Empty);
  rpc Logout(Empty) returns (Empty);
}